fn main(x : Field, y : pub Field) {
    assert(x != y);
}

fn hmac_sha1(key: Field, message: Field) {
    
}

#[test]
fn test_main() {
    main(1, 2);

    // Uncomment to make test fail
    // main(1, 1);
}
